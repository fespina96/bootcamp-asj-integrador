import { PipesExamplePipe } from './pipes-example.pipe';

describe('PipesExamplePipe', () => {
  it('create an instance', () => {
    const pipe = new PipesExamplePipe();
    expect(pipe).toBeTruthy();
  });
});
